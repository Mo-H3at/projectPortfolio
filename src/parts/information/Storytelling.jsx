import { Typography } from "@mui/material"

export default function StoryTelling() {
    return (
        <div>
   <Typography variant="h5" align="center">The main reason they exist</Typography>
   <Typography paragraph>
   </Typography>
   </div>
    )
}